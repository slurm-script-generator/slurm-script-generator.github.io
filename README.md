# slurm-script-generator.github.io

## Building and Running Locally

This project is a static site. To view it locally, you can use a simple HTTP server.

```
python serve_cors.py
```

Then open `index.html` in a browser.